# ねこパズル：制作記録

このファイルは、アプリをどう考え、どう作ったかを残すための記録です。展示では、子供にも読みやすい言葉に要約して使えます。

## 0. はじめに：作るものを決める

### 目的

お祭りに来た子供がiPadで楽しく遊びながら、「Webアプリは人が考えて作っている」ことを知る。

### 選んだ出し物

ねこ写真を使ったジグソーパズル。

### この案を選んだ理由

- 説明を読まなくても、触ってすぐに遊び始められる
- 年齢に合わせてピース数を変えられる
- 猫という会社らしい題材を使える
- 最初の版はサーバーやログインが不要で、短期間で作れる

## 1. 最初の仕様（作る前の設計）

### 遊び方

1. 猫の写真と難易度を選ぶ
2. 分割されたピースを正しい場所へドラッグする
3. 写真が完成したら、お祝いの画面を表示する
4. 「もう一度あそぶ」で最初に戻る

### 最初の版に入れるもの

- 1枚の猫写真
- 3×3（9ピース）のパズル
- ドラッグによるピース配置
- 完成の判定
- リスタートボタン

### 最初の版では入れないもの

- アカウント作成・得点保存
- 写真のアップロード
- 外部サービスとの連携

これらを後回しにすると、個人情報を扱わず、iPadで安定して遊べる最小版を早く完成させられる。ドラッグ操作はパズルの中心となるため、初期版に含める。

## 2. 技術の選び方

HTML、CSS、JavaScriptだけで作る。

- **HTML**：画面にボタンやパズルを並べる
- **CSS**：かわいく、大きく、押しやすい見た目にする
- **JavaScript**：ピースを作る・ドラッグして置く・完成を判定する

特別なソフトを使わず、Safariで開けるWebページにする。iPadにアプリをインストールしなくても使えるのが利点。

## 3. これからの作業

- [ ] 画面のラフを決める
- [ ] 猫写真を用意し、利用許諾を確認する
- [x] 12ピースのパズルを実装する
- [ ] iPadでドラッグ操作を確認する
- [ ] 完成画面とリスタートを追加する
- [ ] 展示用に「どう作ったか」を短くまとめる

## 4. 展示で伝えるメッセージ案

「このゲームは、画面のデザインを考え、写真を9つに分け、完成したかをコンピューターに確かめてもらうプログラムでできています。」

## 更新履歴

| 日付 | 内容 |
| --- | --- |
| 2026-08-19 | 新規プロジェクトを作成し、最初の方針と仕様を記録した。 |
| 2026-08-19 | 実装用の設計書を追加。猫写真を `public/images/cat-images/` に置き、写真一覧をCSVで管理する方針を決定した。 |
| 2026-08-19 | 恒久的な開発用フォルダへ移動し、ローカルGitリポジトリとして管理を開始した。 |
| 2026-08-19 | パズルの形式に合わせ、タイトルを「ねこ絵合わせパズル」に変更した。 |
| 2026-08-19 | 写真一覧のデータ形式をCSVからJSONへ変更した。アプリから扱いやすく、項目の追加にも対応しやすい形式を選んだ。 |
| 2026-08-19 | 写真をシャッフルして交換する方式から、写真を分割したピースを正しい位置へドラッグするジグソーパズル方式へ変更した。 |
| 2026-08-19 | JSONから猫写真を読み込み、3×3に分割したピースをドラッグして置くパズルを実装した。盤面には薄い完成見本を表示し、写真そのものはシャッフルしない。 |
| 2026-08-19 | タイトルを「ねこパズル」に変更した。 |
| 2026-08-19 | 盤面を横4列・縦3行の12ピースへ変更した。 |
| 2026-08-19 | DockerとNginxを使い、`docker compose up --build` でアプリを配信できる設定を追加した。 |
| 2026-08-19 | 画像と写真一覧JSONの参照をルート相対パスへ変更し、どの画面URLから開いても正しく読み込めるようにした。 |
| 2026-08-19 | 誤った枠でピースを離した場合、ドラッグ開始時の置き場と順番へ必ず戻す処理を追加した。 |
| 2026-08-19 | `app.js` の主要な関数に、日本語で役割を説明するコメントを追加した。 |
| 2026-08-19 | 指を離した時・ドラッグが中断された時に、ドラッグ用プレビューを必ず削除する処理を追加した。 |
| 2026-08-19 | 盤面の境界線付近でピースを離した場合、最も中心に近い枠へ自動的に吸着する処理を追加した。 |
| 2026-08-19 | ピース置き場の行が伸びないようにし、残りピース数にかかわらずピースの表示サイズを一定にした。 |
| 2026-08-19 | 猫写真の選択をラジオボタンに変更し、常に1枚だけ選べるようにした。 |
| 2026-08-19 | 正しい枠に置けず置き場へ戻ったピースを点滅させ、次に操作するピースを分かりやすくした。 |
| 2026-08-19 | 置けなかったピースの点滅が約3秒後に自動で終わるようにした。 |
| 2026-08-19 | 成功・失敗にかかわらず、最後に置いたピースを薄くなる点滅で約3秒表示するように変更した。 |
| 2026-09-11 | iPad横向きの全画面をブラウザのツールバー表示時の高さ（100svh）に合わせて調整。猫写真は4列、ピースは全難易度で全枚表示し、完成写真と操作ボタンも画面内に配置。Chromeの表示領域1024×507・1024×600・1180×720で選択・全難易度・完成画面のはみ出しとページスクロールがないことを検証。iPad Safari実機での確認は未実施。 |
